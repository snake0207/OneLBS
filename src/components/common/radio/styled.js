import styled from '@emotion/styled'

export const RadioInputContainer = styled.section`
    margin: 10px 0;
`

export const RadioLabelContainer = styled.section`
    display: flex;
    justify-content: space-between;
`

export const RadioLabel = styled.p`
    font-size: 20px;
    font-weight: 500;
    margin: 10px 0;
`

export const RadioButton = styled.button`
    flex: 0 0 auto;
    padding: 6px 30px;
    font-size: 16px;
    font-weight: 600;
    background-color: white;
    border: 1px solid lightgray;
    cursor: pointer;
`

export const RequiredSpan = styled.span`
    font-size: inherit;
    color: red;
`
